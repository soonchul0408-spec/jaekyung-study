# 재경관리사 기출 노트 — 작업 기준

이 파일은 이 저장소에서 작업할 때 가장 먼저 읽는 기준 문서다. 새 회차 추가, 해설 보완, 화면 수정, 품질검사와 배포는 아래 규칙을 따른다.

## 운영 정보

- 운영 사이트: https://jaekyung-study.vercel.app
- GitHub: https://github.com/soonchul0408-spec/jaekyung-study
- 기본 브랜치: `main`
- 현재 등록 회차: 2025년 1월·3월·5월
- 회차 구성: 과목별 40문항, 총 120문항
- 기준 회차: 2025년 1월
- 원칙: 새 회차와 새 기능은 1월의 화면·학습 기능·해설 품질과 동등하거나 그 이상이어야 한다. 일부 기능만 연결하거나 해설·빈출 연결·검사를 생략한 상태로 배포하지 않는다.

## 먼저 확인할 문서

| 문서 | 확인 목적 |
|---|---|
| `docs/QUALITY_CHECKLIST.md` | 회차 추가·해설 수정의 배포 전 품질검사표 |
| `docs/EXPLANATION_QUALITY_STANDARD.md` | 이론형·계산형 해설의 작성 및 검증 기준 |
| `docs/SITE_STRUCTURE_GUIDE.md` | 기존 화면 구조, 디자인 톤, 모바일 UX 기준 |

위 문서의 내용이 이 파일보다 구체적인 경우에는 그 문서의 기준을 따른다.

## 핵심 구조

| 파일 | 역할 |
|---|---|
| `src/data/examSets.js` | 모든 회차의 단일 등록 목록. 새 회차는 반드시 여기 등록한다. |
| `src/data/questions.js` | 2025년 1월 문항 |
| `src/data/questions-2025-03.js` | 2025년 3월 문항 |
| `src/data/questions-2025-05.js` | 2025년 5월 문항 |
| `scripts/buildMarchExamData.mjs` | `EXAM_MONTH` 기반 공용 문항 생성기와 계산형 풀이 데이터 |
| `src/data/frequencyAnalysis.js` | 빈출 TOP 12 분석 기준과 실제 문항 ID 연결 |
| `src/main.js` | 월별·항목별 화면, 문제 화면, 저장 상태와 화면 동작 |

문항 ID는 `YYYY-MM-{FR|TX|CM}-{01..40}` 형식을 사용한다. 기존 문항의 ID·정답·분류·해설을 화면 개선 목적으로 삭제하거나 임의 변경하지 않는다.

## 새 회차 추가 절차

1. 공식 문제지와 확정답안만 원본으로 확보한다.
2. 문제·답안을 각각 `/private/tmp/jaekyung-YYYY-MM-questions.txt`, `...-answers.txt`로 추출한다.
3. `EXAM_MONTH=YYYY-MM node scripts/buildMarchExamData.mjs`로 데이터를 만든다.
4. 과목별 40문항·총 120문항·4지선다·확정답안을 확인한다.
5. `src/data/examSets.js`에 회차를 등록한다.
6. 모든 문항에 taxonomy, 근거, 풀이 방향, 해설, 보기별 분석을 작성한다.
7. 실제 문항을 근거로만 `frequencyAnalysis.js`의 TOP 12 `problemIds`를 연결한다.
8. 아래 품질검사와 빌드를 전부 통과한 뒤에만 배포한다.

## 해설 품질: 모든 문항의 전수 기준

### 전문가 감수 완료 — 명품 해설 기준

자동 검사 통과는 전문가 감수 완료가 아니다. 유료 해설집으로 제공해도 되는 수준이라고 표시하거나 배포하려면 아래 기준을 문항별로 모두 충족해야 한다.

1. **독립 풀이와 원문 충실성**: 문제지·확정답안을 기준으로 먼저 독립적으로 정답 논리 또는 계산값을 확정한 뒤 해설을 작성한다. 생성된 해설이나 기존 선택지 분석을 정답 근거로 삼지 않는다.
2. **선택지-해설 일치**: `answer` 번호, 해당 선택지의 실제 표현, `choiceAnalysis`의 논점·결론이 정확히 같은 문항을 가리키는지 대조한다. 다른 번호의 선택지·다른 문항의 계산·다른 단원의 설명을 재사용하면 실패다.
3. **말문제의 설명 순서**: 정답 선택지의 실제 표현 → 적용 기준·요건·예외 → 해당 지문에서의 판단 → 수험생 실수 포인트 순으로 쓴다. ‘옳지 않은 것’은 틀린 표현과 대체되는 정확한 기준을 반드시 함께 쓴다.
4. **실전 풀이방향**: `direction`과 `solutionSteps`에는 해당 지문의 주체·기간·수치·부정 표현·예외를 확인하는 실제 순서를 쓴다. “기준을 대입한다”, “수치·기간을 분리한다” 같은 회차·문항 공통 템플릿만으로는 완료할 수 없다.
5. **계산의 독립 검산**: 계산형은 식을 독립 재계산한다. 모든 등식의 좌우, 중간값, 단위, 기간, 부호, 포함·제외 항목과 최종 선택지를 확인한다. 산술이 맞지 않거나 중간값에서 최종값으로의 연결이 생략되면 실패다.
6. **충돌의 공개 보류**: 원문·확정답안·계산 논리 사이에 충돌하거나 근거가 부족하면 정답을 임의 수정하지 않는다. `sourceVerificationNote`에 선택지, 원문 조건, 독립 계산값과 충돌 원인을 적고 `reviewNeeded: true`, `explanationStatus: 'review'`로 둔다.
7. **문항별 감수 기록**: 자동검사 후에도 ‘선택지-해설 대응’, ‘수치 재계산 또는 규정 대조’, ‘이 문항만 읽고 판단 가능한지’를 전수 확인한다. 세 항목 중 하나라도 실패하면 전문가 감수 완료로 표시하지 않는다.

이 기준은 정답 해설 중심 범위 원칙을 보완한다. 말문제의 오답별 장문 해설은 필수가 아니지만, 정답 해설·풀이방향·계산이 실제 문항과 정확히 대응해야 한다.

### 2025년 1월 모범 절차 — 이후 회차 의무 적용

2025년 1월 회차에서 정립한 아래 순서를 이후 추가하는 모든 회차의 생성·감수 절차로 사용한다.

1. 문제·확정답안을 입력하고, 문항별 `answer`와 정답 선택지 원문을 먼저 고정한다.
2. 정답 선택지 하나에 대해 실제 표현, 판단 기준·예외, 지문 적용, 실수 포인트를 작성한다. 말문제에는 오답별 장문 해설을 덧붙이지 않는다.
3. `direction`과 `solutionSteps`에는 지문의 핵심 조건과 정답 선택지 원문을 함께 넣어, 정답을 고르는 실제 순서를 만든다.
4. 계산형은 독립 계산 후 `formula`·`substitutions`·`result`와 수험생용 설명을 작성하고 선택지와 검산한다.
5. 생성기에서는 `expertLearningDataFor`를 사용해 정답 해설, 판단 순서, 실수 포인트가 같은 정답 선택지를 가리키게 한다. 회차별 수동 해설이 있으면 일반 템플릿보다 우선한다.
6. 확정답안과 독립 계산이 다르면 원문 답안을 보존하고 `sourceVerificationNote`, `reviewNeeded`, `explanationStatus: 'review'`를 함께 기록한다. 검토 문항도 화면에 공식답안·해설·검토 사유를 함께 표시한다.
7. 필수 자동검사 후 120문항의 정답 번호·선택지·해설 대응 및 계산 검산을 전수 확인한다.

### 최우선 범위 원칙 — 정답 해설 중심

- 8회차 전체 문항의 해설은 **정답 선택지의 실제 표현·판단 근거·적용 기준·예외·실수 포인트**를 명확히 설명하는 데 집중한다.
- 개념·판단형 말문제는 정답 선택지 해설만 필수 작성 대상이다. 오답 선택지 ①~④ 각각의 상세 해설은 새로 작성하지 않으며, 기존 상세 오답 해설을 전수 유지·보완 대상으로 삼지 않는다.
- 화면의 선택지 분석은 정답 선택지 중심으로 제공한다. 오답은 수험생이 직접 검토할 수 있도록 정답 해설의 판단 기준과 풀이 방향만 제공하며, 공통 문구로 오답 해설을 채워 넣지 않는다.
- 계산형은 이 범위 축소의 예외다. 무엇을 구하는지, 산식, 두 단계 이상의 수치 대입·중간값, 최종값·정답 선택지 대조, 단위·기간·부호·포함·제외 실수 포인트를 계속 모두 제공한다.
- 이 원칙은 기존 2025년 1·3·5월과 이후 추가하는 회차를 포함한 총 8회차 전체에 최우선 적용한다. 다른 문서의 선택지별 상세 해설 요구와 충돌하면 이 원칙을 우선한다.

- 공식 확정답안과 일치해야 하며, 해설은 반드시 `AI 학습용 해설`로 표시한다.
- 지문과 ①~④ 보기의 실제 표현을 근거로 사용한다.
- 풀이 방향에는 실제 핵심 표현, 구체적 판단 기준, 적용 순서를 모두 넣는다.
- 정답 및 해설에는 정답 원리, 실제 정답 선택지 표현, 보기별 분석, 수험생 실수 포인트를 모두 넣는다.

### 개념·판단형, 특히 ‘옳지 않은 것’

- 모든 선택지는 맞음·틀림의 이유를 실제 단원 기준으로 설명한다.
- ‘옳지 않은 것’의 정답 선택지는 반드시 `틀림(정답)`으로 표시한다.
- 정답 설명은 **틀린 문구 → 정확한 올바른 기준·수치·목록·요건 → 이 선택지가 정답인 이유** 순서로 쓴다.
- “단원 기준과 맞는지 확인한다”, “기준과 충돌한다”처럼 올바른 기준을 생략한 문장은 금지한다.
- 예: “신뢰성·보수성”이 아니라 “비교가능성·검증가능성·적시성·이해가능성”이 보강적 질적 특성이므로 해당 선택지가 틀렸다고 설명한다.

### ‘모든 회차 동일 기준 적용’의 의미

사용자가 특정 문항을 예시로 들어 해설·선택지 분석·풀이 방향·계산과정을 상세 보완한 뒤 “모든 회차에 동일 기준을 적용하라”고 요청하면, 이는 형식·필드·문장 길이만 맞추라는 뜻이 아니다. 예시 문항에서 보완한 **내용의 구체성**을 같은 범위의 모든 문항에 적용해야 한다.

예를 들어 ‘옳지 않은 것’ 문항의 선택지 분석을 보완한 경우, 모든 등록 회차의 같은 유형 문항마다 다음을 작성한다.

1. 실제로 틀린 선택지의 정확한 문구 또는 핵심 표현
2. 그 표현이 틀린 이유
3. 해당 문항에 적용되는 정확한 올바른 기준·수치·목록·요건
4. 그 기준 때문에 해당 선택지가 정답이 되는 논리

단원명, 일반적인 판단 원칙, “기준과 대조한다” 같은 공통 문구를 문항별 설명 대신 반복해서는 안 된다. 예시 문항보다 구체성이 낮은 해설은 완료로 처리하지 않는다.

동일 기준 적용 요청을 받은 뒤에는 대상 회차와 대상 문항을 전수 식별하고, 실제 지문·선택지·확정답안을 근거로 작성한다. 형식 검사와 별도로 공통 템플릿·빈 설명·일반론만 남은 문항이 없는지 전수 점검하며, 하나라도 남으면 배포·완료 보고를 하지 않는다.

### 계산형

모든 계산형에는 다음을 전부 작성한다.

1. 무엇을 구하는 문제인지
2. 적용 기준과 계산식
3. 두 단계 이상의 수치 대입과 중간값
4. 최종값과 정답 선택지 대조
5. 단위·기간·부호·포함/제외 등 실수 방지 포인트

`calculation`에는 `formula`, 두 개 이상의 `substitutions`, `result`, `verifiedAgainstAnswer: true`가 필요하다. 원문·확정답안과 충돌하거나 근거가 부족하면 `reviewNeeded: true`, `explanationStatus: review`로 두고 공개 완료로 처리하지 않는다.

### 수험생 관점 해설 품질

자동 검사가 통과했더라도, 수험생이 해당 문항만 보고 정답 논리와 오답 원인을 이해할 수 없으면 품질검사 통과로 처리하지 않는다.

1. ①~④ 모든 선택지는 실제 선택지 표현을 근거로 맞음·틀림의 이유와 적용 기준·예외를 직접 설명한다. “지문 조건과 대조한다”, “정답과 일치하지 않는다”, “단원 기준을 확인한다” 같은 공통 문구만으로 끝내면 안 된다.
2. 풀이 방향은 단원 일반론이 아니라 해당 문항의 실제 풀이 순서다. 지문에서 확인할 숫자·기간·주체·예외, 적용할 기준 또는 공식, 선택지를 제거하거나 정답을 확정하는 순서를 문항별로 제시한다.
3. 계산형 여부는 키워드가 아니라 실제 수치 풀이 필요성으로 판정한다. 계산형에는 산식·수치 대입·중간값·최종값·정답 선택지 대조를 제공하고, 판단형에는 계산 필드를 형식적으로 채우지 않는다.
4. 해설의 사실관계와 내부 논리를 별도로 검토한다. 기준·수치·부등호·기간·분류가 원문·확정답안과 일치하고 서로 모순되지 않아야 한다.
5. 사용자가 “모든 회차”, “전 문항”, “동일 기준 적용”을 요청한 경우에는 정답 선택지 일부만 보완하거나 문장 형식만 통일하거나 자동검사만 통과한 상태를 완료로 처리하지 않는다. 공통 템플릿·근거 없는 설명·사실 오류·내부 모순이 하나라도 남으면 배포 및 완료 보고를 하지 않는다.

## 빈출·화면 기준

- `frequencyAnalysis.js`에는 실제 존재하고 확인된 문항만 연결한다. 중복 ID·없는 ID·과목 불일치를 허용하지 않는다.
- 현재 베이지 배경, 짙은 녹색 강조, 주황 포인트, 둥근 카드·버튼 톤을 유지한다.
- 모바일에서 탭·필터·배지·버튼이 깨지지 않아야 하며, 중요 상태는 색과 텍스트를 함께 표시한다.
- 한국어 단어가 중간에서 끊기지 않도록 PDF 줄바꿈을 정리한다. 표 행 구조는 보존한다.
- 긴 목록과 근거·풀이·해설은 기본 접힘으로 제공하고, 열어도 화면이 맨 위로 이동하지 않아야 한다.
- 연도별 기출은 월별 진입, 항목별 복습은 전 회차 통합 복습이라는 역할을 유지한다.
- 새 회차에도 월별 선택, 항목별 통합 복습, 빈출 배지, 헷갈리는 문제 저장, 근거·풀이·해설 접기, 모바일 반응형을 모두 제공한다.
- 기존 `localStorage` 상태를 보존한다.

## 필수 검증과 완료 규칙

새 회차 추가·해설 수정·데이터 구조 수정 후에는 다음을 모두 통과해야 한다.

```bash
npm run check:frequency
npm run check:exam-parity
npm run check:exam-quality
npm run check:explanations
npm run audit:student-explanations
npm run build
```

하나라도 실패하면 배포하지 않는다. 자동 검사를 통과해도 원문·확정답안과 충돌하거나 설명이 빈약하다고 판단되면 전수 검토 후 수정한다.

완료 조건이 남거나 검사가 실패한 상태에서는 최종 응답을 보내지 않는다. 최종 보고·배포 전에는 남은 문항과 누락 필드, 필수 검증 전체, `git status`, 요청된 커밋·GitHub 푸시·Vercel 배포를 한 번에 확인한다.

사용자가 “모든 회차에 적용해서 구현”처럼 완료형 작업을 지시하면, 미완료 상태·보류 여부를 반복 보고하지 않는다. 실제 전수 작업과 검증을 끝낸 뒤 결과를 한 번에 보고한다. 원본 자료·권한·외부 상태처럼 에이전트가 단독으로 해결할 수 없는 중대한 차단 사유만 즉시 알린다.

배포 명령:

```bash
npx vercel --prod
```

권한 오류가 나면 `npx vercel login` 후 재시도한다.
